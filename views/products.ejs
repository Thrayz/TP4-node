<% products.forEach(function(product) { %>
    <h1><%= product.name %></h1>
    <p><%= product.description %></p>
    <p><%= product.price %></p>
    <p>Categorie: <%= product.categorie.name %></p>
<% }); %>